name = "mentalist-stars-backend"
main = "src/worker.ts"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

[vars]
NODE_ENV = "production"

# SECURITY NOTE:
# API keys are stored in .dev.vars for local development (NOT committed to Git)
# For production deployment, use: npx wrangler secret put OPENROUTER_API_KEY
